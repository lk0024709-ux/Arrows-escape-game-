name: arrows_escape_game
description: "Arrows Escape Game in Flutter"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Agar game ke images/audio hain toh yahan unka path add karein:
  # assets:
  #   - assets/images/
  #   - assets/audio/
